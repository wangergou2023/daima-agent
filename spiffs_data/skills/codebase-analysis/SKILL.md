---
name: 代码库分析
description: 当用户要求分析当前工程、某个仓库、模块结构、入口、主流程或迁移路径时使用。
---

# 代码库分析

分析代码仓库时先建立结构地图，再定点读取关键文件，避免无目标地阅读全文。

## 何时使用

- 用户要求分析当前目录、工程、仓库或模块。
- 用户想知道入口、模块划分、主流程、依赖关系。
- 用户准备移植功能，想定位核心文件和关键链路。

## 使用步骤

1. 先用 `terminal` 查看目录结构和构建文件。
2. 再按关键词搜索入口、注册表、主循环、工具名、配置名和外部接口。
3. 用 `files action=read` 分页读取最相关文件。
4. 优先识别构建入口、进程入口、调度主链路、能力注册、外部接口、状态存储。
5. 如果用户只要概览，读到足够支撑结论时停止扩大范围。

## 工具与路径

- 常用工具：`terminal`、`files action=read`、`skills action=list`、`skills action=view`。
- 常见搜索目标：`main`、`init`、`agent`、`registry`、`router`、`tool`、`config`。

## 输出要求

- 说明项目是什么、主入口在哪里、核心模块有哪些。
- 给出关键数据流或调用链。
- 如果用户要改功能，列出应该优先看的文件。

## 注意事项

- 不要默认把整个仓库所有源码全文读完。
- 头文件通常只在确认接口、结构体、宏定义时再读。
- 回答优先讲结构和主链路，不做逐文件流水账。
