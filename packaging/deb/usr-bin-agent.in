#!/bin/sh
set -eu

APP_DIR="/usr/lib/agent"
AGENT_SEED_DIR="/usr/share/agent/spiffs_data"
AGENT_HOME="${AGENT_HOME:-$HOME/.agent-data}"

export AGENT_SEED_DIR
export AGENT_HOME
"$APP_DIR/init-home.sh"
exec "$APP_DIR/bin/agent" "$@"
